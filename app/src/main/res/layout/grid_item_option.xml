<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="60dp"
    android:layout_height="55dp"
    android:textAlignment="center"
    android:gravity="center"
    android:layout_gravity="center|top"
    >
    <ImageView
        android:id="@+id/img_grid_option"
        android:layout_width="wrap_content"
        android:layout_height="26dp"
        android:src="@drawable/ic_option_air"
        android:scaleType="fitEnd"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toTopOf="@id/txt_grid_option"
        />
    <TextView
        android:id="@+id/txt_grid_option"
        android:layout_width="wrap_content"
        android:layout_height="18dp"
        app:layout_constraintTop_toBottomOf="@id/img_grid_option"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="에어컨"
        android:textSize="12dp"/>


</androidx.constraintlayout.widget.ConstraintLayout>
</layout>